# Analiza Skills dla Systemu Agentów Claude Code

Data analizy: 2025-11-16

## 1. PODSUMOWANIE WYKONAWCZE

Przeanalizowano 9 agentów i 4 standardy współdzielone, identyfikując:
- **10 powtarzających się wzorców** gotowych do wyodrębnienia jako skills
- **15 nowych skills** które mogłyby rozszerzyć możliwości systemu
- **Potencjalne oszczędności**: ~30-40% redukcja duplikacji kodu w definicjach agentów

---

## 2. ISTNIEJĄCE AGENTY - PRZEGLĄD

### Higher-Order Agents (Orchestratorzy)
- **mvp-manager** (Opus) - zarządzanie workflow MVP/features
- **error-manager** (Opus) - zarządzanie workflow naprawy błędów

### Planner Agents (Architekci/Analitycy)
- **code-architect** (Opus) - projektowanie architektury
- **interaction-mapper** (Sonnet) - mapowanie interakcji
- **error-tracker** (Opus) - analiza wzorców błędów

### Doer Agents (Implementatorzy)
- **quick-developer** (Sonnet) - szybkie MVP
- **quality-developer** (Sonnet) - produkcyjny kod
- **test-developer** (Sonnet) - testy

### Optimizer Agents
- **project-optimizer** (Opus) - optymalizacja agentów

---

## 3. SKILLS DO WYODRĘBNIENIA Z ISTNIEJĄCYCH AGENTÓW

### 3.1 🔥 JSON Report Management (WYSOKI PRIORYTET)

**Występuje w:** Wszystkich 9 agentach + JSON-REPORTING-STANDARD.md

**Obecna implementacja:** Każdy agent ma ~150 linii kodu definiującego strukturę JSON

**Co skill powinien robić:**
```yaml
skill: json-report-manager
capabilities:
  - Generowanie szablonów raportów JSON per typ agenta
  - Walidacja struktury raportów według standardu
  - Automatyczne wypełnianie metadanych (timestamp, session_id, agent_name)
  - Aktualizacja raportów (dodawanie delegacji, errors, files_modified)
  - Obliczanie metryk (duration, lines_added/removed, etc.)

inputs:
  - agent_type: "orchestrator | planner | doer"
  - session_id: string
  - task_description: string

outputs:
  - json_report_template: object
  - validation_results: object

benefits:
  - Eliminacja ~1500 linii duplikacji
  - Gwarancja spójności raportów
  - Łatwiejsze wprowadzanie zmian do standardu
```

**Przykład użycia:**
```
Skill: json-report-manager
Action: create_report
Params:
  agent_type: doer
  agent_name: quick-developer
  session_id: 1611_2025_01
  requested_by: mvp-manager
```

---

### 3.2 🔥 Agent Signature Generator (WYSOKI PRIORYTET)

**Występuje w:** Wszystkich agentach + INTER-AGENT-COMMUNICATION-GUIDELINES.md

**Obecna implementacja:** Każdy agent definiuje 3-4 różne formaty sygnatur

**Co skill powinien robić:**
```yaml
skill: agent-signature-generator
capabilities:
  - Generowanie sygnatur dla różnych typów plików (MD, JS, PY, JSON, YAML, etc.)
  - Automatyczne wstawianie metadanych (agent, timestamp, session, purpose)
  - Walidacja poprawności sygnatur w istniejących plikach
  - Batch signing - podpisywanie wielu plików naraz

inputs:
  - file_type: "markdown | code | json | yaml"
  - agent_name: string
  - session_id: string
  - purpose: string
  - requested_by: string (optional)

outputs:
  - signature_block: string
  - validation_status: boolean

benefits:
  - Eliminacja ~600 linii duplikacji
  - Spójne sygnatury we wszystkich plikach
  - Automatyczna traceability
```

**Przykład:**
```
Skill: agent-signature-generator
Action: generate
Params:
  file_type: javascript
  agent_name: quick-developer
  session_id: 1611_2025_01
  purpose: "MVP authentication implementation"
  requested_by: mvp-manager

Output:
/**
 * Generated by QUICK-DEVELOPER - 2025-11-16 10:30
 * Session: 1611_2025_01 | Requested by: mvp-manager
 * Purpose: MVP authentication implementation
 */
```

---

### 3.3 🔥 Session Manager (WYSOKI PRIORYTET)

**Występuje w:** mvp-manager, error-manager + ORCHESTRATION-ENFORCEMENT.md

**Obecna implementacja:** ~300 linii w każdym orchestratorze + ręczna walidacja

**Co skill powinien robić:**
```yaml
skill: session-manager
capabilities:
  - Skanowanie istniejących folderów sesji
  - Walidacja nazw (DDMM_YYYY_NN bez sufixów)
  - Automatyczne znajdowanie następnego numeru sekwencyjnego
  - Tworzenie struktury folderów sesji
  - Generowanie session-metadata.json
  - Walidacja globalnej sekwencji (cross-agent)

inputs:
  - action: "list | validate | create_next | get_metadata"
  - agent_name: string (dla create_next)

outputs:
  - next_session_id: string
  - existing_sessions: array
  - validation_errors: array
  - session_path: string

benefits:
  - Zero błędów w nazewnictwie sesji
  - Eliminacja konfliktów między agentami
  - Automatyczna walidacja przed utworzeniem
```

**Przykład:**
```bash
# Obecny stan - ręczna walidacja:
ls -la project/internal/  # pokazuje 1611_2025_01, 1611_2025_02
# Ręczne obliczenie: 02 + 1 = 03
mkdir project/internal/1611_2025_03

# Ze skill'em:
Skill: session-manager
Action: create_next
Params:
  agent_name: error-manager

Output:
  session_id: "1611_2025_03"
  session_path: "project/internal/1611_2025_03"
  validation: "passed"
  previous_sessions: ["1611_2025_01 (mvp-manager)", "1611_2025_02 (error-manager)"]
```

---

### 3.4 🟡 Build Validator (ŚREDNI PRIORYTET)

**Występuje w:** quick-developer, quality-developer, mvp-manager, error-manager + BUILD-VALIDATION.md

**Obecna implementacja:** ~400 linii w każdym agencie

**Co skill powinien robić:**
```yaml
skill: build-validator
capabilities:
  - Wykrywanie typu projektu (.NET, Node.js, Python, etc.)
  - Uruchamianie odpowiednich build commands
  - Parsowanie wyników buildu (errors, warnings)
  - Formatowanie błędów do JSON
  - Uruchamianie testów po sukcesie buildu
  - Parsowanie wyników testów
  - Generowanie raportu build validation

inputs:
  - project_path: string (default: ".")
  - build_type: "auto | dotnet | nodejs | python" (default: auto)
  - run_tests: boolean (default: true)
  - iteration_number: number

outputs:
  - build_status: "success | failure"
  - build_errors: array
  - build_warnings: array
  - test_status: "success | failure | skipped"
  - test_results: object
  - build_report_json: object

benefits:
  - Spójne raportowanie buildów
  - Automatyczne wykrywanie typu projektu
  - ~1200 linii duplikacji usuniętych
```

**Przykład:**
```
Skill: build-validator
Action: validate
Params:
  project_path: "."
  run_tests: true
  iteration_number: 1

Output:
{
  "build_status": "success",
  "build_command": "dotnet build",
  "build_warnings": [
    {"file": "src/Auth.cs", "line": 45, "code": "CS0219", "message": "Variable never used"}
  ],
  "test_status": "success",
  "tests_run": 42,
  "tests_passed": 42
}
```

---

### 3.5 🟡 File Naming Validator (ŚREDNI PRIORYTET)

**Występuje w:** Wszystkich agentach + INTER-AGENT-COMMUNICATION-GUIDELINES.md

**Obecna implementacja:** Opisowa, bez walidacji

**Co skill powinien robić:**
```yaml
skill: file-naming-validator
capabilities:
  - Walidacja formatu PPP-SSS-descriptive-name.ext
  - Sugerowanie nazw na podstawie fazy i opisu
  - Wykrywanie konfliktów nazw
  - Generowanie listy plików dla fazy
  - Sprawdzanie sekwencji numeracji w fazie

inputs:
  - file_name: string
  - phase: number (000-999)
  - session_path: string
  - action: "validate | suggest | list_phase"

outputs:
  - is_valid: boolean
  - validation_errors: array
  - suggested_name: string
  - existing_files_in_phase: array

benefits:
  - Spójność nazewnictwa plików
  - Łatwiejsze nawigowanie po sesjach
```

---

### 3.6 🟡 User Parameters Manager (ŚREDNI PRIORYTET)

**Występuje w:** Wszystkich agentach

**Obecna implementacja:** Każdy agent definiuje swoje domyślne parametry

**Co skill powinien robić:**
```yaml
skill: user-params-manager
capabilities:
  - Czytanie user-params.json z sesji
  - Walidacja struktury parametrów
  - Dostarczanie domyślnych wartości per agent
  - Mergowanie global_params z agent_params
  - Generowanie szablonów user-params.json

inputs:
  - session_path: string
  - agent_name: string
  - action: "read | validate | create_template"

outputs:
  - parameters: object
  - validation_results: object
  - merged_params: object (global + agent-specific)

benefits:
  - Centralne zarządzanie parametrami
  - Spójne defaulty
  - Łatwa walidacja
```

---

### 3.7 🟢 Path Validator (NISKI PRIORYTET)

**Występuje w:** mvp-manager, error-manager + ORCHESTRATION-ENFORCEMENT.md

**Obecna implementacja:** ~100 linii w każdym orchestratorze

**Co skill powinien robić:**
```yaml
skill: path-validator
capabilities:
  - Walidacja czy path jest dozwolony do zapisu
  - Sprawdzanie czy path to kod aplikacji
  - Weryfikacja dla różnych typów agentów

inputs:
  - path: string
  - agent_type: "orchestrator | planner | doer"
  - operation: "read | write | execute"

outputs:
  - is_allowed: boolean
  - reason: string
  - suggested_alternative: string (jeśli zabroniony)

benefits:
  - Enforcement separation of concerns
  - Łapanie błędów przed operacją
```

---

### 3.8 🟢 Delegation Tracker (NISKI PRIORYTET)

**Występuje w:** mvp-manager, error-manager

**Co skill powinien robić:**
```yaml
skill: delegation-tracker
capabilities:
  - Rejestrowanie delegacji do innych agentów
  - Aktualizacja statusu delegacji
  - Generowanie 999-002-agent-handoffs.json
  - Wykrywanie cykli delegacji
  - Obliczanie duration dla delegacji

inputs:
  - action: "register | update | finalize"
  - from_agent: string
  - to_agent: string
  - purpose: string
  - input_files: array
  - output_files: array

outputs:
  - delegation_id: string
  - handoff_record: object
  - audit_trail: array
```

---

### 3.9 🟢 Error Pattern Analyzer (NISKI PRIORYTET)

**Występuje w:** error-tracker

**Co skill powinien robić:**
```yaml
skill: error-pattern-analyzer
capabilities:
  - Parsowanie stack traces
  - Identyfikacja wzorców błędów
  - Mapowanie propagacji błędów
  - Sugerowanie root cause
  - Znajdowanie podobnych błędów w historii

inputs:
  - error_message: string
  - stack_trace: string
  - file_path: string (optional)
  - search_history: boolean

outputs:
  - error_type: string
  - confidence: number
  - root_cause_suggestion: string
  - similar_errors: array
  - propagation_path: array
```

---

### 3.10 🟢 Code Search Helper (NISKI PRIORYTET)

**Występuje w:** Wielu agentów używających Grep/Glob

**Co skill powinien robić:**
```yaml
skill: code-search-helper
capabilities:
  - Predefiniowane search patterns (funkcje, klasy, imports)
  - Smart search z context
  - Agregacja wyników z wielu plików
  - Highlighting i formatowanie wyników

inputs:
  - search_type: "function | class | import | pattern | error"
  - search_term: string
  - scope: "file | directory | project"
  - context_lines: number

outputs:
  - matches: array
  - formatted_results: string
  - statistics: object
```

---

## 4. NOWE SKILLS DO DODANIA

### 4.1 🔥 Git Operations Manager (WYSOKI PRIORYTET)

**Uzasadnienie:** Każdy agent może potrzebować operacji git

**Funkcjonalność:**
```yaml
skill: git-ops-manager
capabilities:
  - Bezpieczne commity z walidacją
  - Tworzenie branchy zgodnie z konwencją
  - Sprawdzanie statusu bez parse'owania
  - Generowanie commit messages zgodnie z conventional commits
  - Walidacja przed push (pre-push checks)
  - Analiza git history dla pliku/modułu

inputs:
  - action: "status | commit | branch | history | validate"
  - files: array (dla commit)
  - message: string (dla commit)
  - branch_name: string (dla branch)

outputs:
  - status: object
  - commit_sha: string
  - validation_results: object

safety_checks:
  - Nigdy nie force push do main/master
  - Zawsze sprawdzaj authorship przed amend
  - Nigdy nie commituj secrets (.env, credentials)
  - Użyj heredoc dla commit messages
```

**Przykład:**
```
Skill: git-ops-manager
Action: commit
Params:
  files: ["src/auth/login.js", "src/auth/register.js"]
  message_type: "feature"
  description: "Add JWT authentication"

Output:
  commit_sha: "a1b2c3d4"
  message: "feat: Add JWT authentication\n\nImplemented login and registration endpoints with JWT token generation"
  files_committed: 2
  safety_checks_passed: true
```

---

### 4.2 🔥 Dependency Analyzer (WYSOKI PRIORYTET)

**Uzasadnienie:** Przydatny dla architektów i developerów

**Funkcjonalność:**
```yaml
skill: dependency-analyzer
capabilities:
  - Analiza import/require statements
  - Wykrywanie circular dependencies
  - Znajdowanie unused dependencies
  - Generowanie dependency graph
  - Sugerowanie refactoringu
  - Identyfikacja outdated packages

inputs:
  - scope: "file | module | project"
  - file_path: string (jeśli scope=file)
  - check_circular: boolean
  - check_unused: boolean

outputs:
  - dependencies: object
  - circular_deps: array
  - unused_deps: array
  - dependency_graph: object
  - recommendations: array
```

---

### 4.3 🟡 Code Metrics Calculator (ŚREDNI PRIORYTET)

**Funkcjonalność:**
```yaml
skill: code-metrics-calculator
capabilities:
  - Cyclomatic complexity
  - Lines of code (LOC, SLOC)
  - Function/method count
  - Class count
  - Coupling metrics
  - Cohesion metrics
  - Maintainability index

inputs:
  - file_path: string
  - metric_types: array ["complexity", "loc", "coupling"]

outputs:
  - metrics: object
  - health_score: number (0-100)
  - recommendations: array
```

---

### 4.4 🟡 Documentation Generator (ŚREDNI PRIORYTET)

**Funkcjonalność:**
```yaml
skill: documentation-generator
capabilities:
  - Generowanie JSDoc/docstrings z kodu
  - Tworzenie README.md z template
  - API documentation z endpoints
  - Changelog generation z git history
  - Architecture diagrams (mermaid) z kodu

inputs:
  - doc_type: "api | readme | changelog | inline"
  - source_files: array
  - output_format: "markdown | html | json"

outputs:
  - documentation: string
  - diagrams: array
```

---

### 4.5 🟡 Test Coverage Analyzer (ŚREDNI PRIORYTET)

**Funkcjonalność:**
```yaml
skill: test-coverage-analyzer
capabilities:
  - Parsowanie coverage reports (Istanbul, Coverage.py, etc.)
  - Identyfikacja uncovered lines/functions
  - Sugerowanie testów do napisania
  - Obliczanie coverage trends
  - Generowanie coverage reports

inputs:
  - project_type: "nodejs | python | dotnet"
  - coverage_file: string
  - threshold: number (default: 80)

outputs:
  - coverage_percentage: number
  - uncovered_files: array
  - uncovered_functions: array
  - test_suggestions: array
  - meets_threshold: boolean
```

---

### 4.6 🟡 Security Scanner (ŚREDNI PRIORYTET)

**Funkcjonalność:**
```yaml
skill: security-scanner
capabilities:
  - Wykrywanie SQL injection vulnerabilities
  - XSS detection
  - Hardcoded secrets detection
  - Insecure dependencies (npm audit, safety)
  - OWASP Top 10 checks

inputs:
  - scan_type: "code | dependencies | config | all"
  - severity_threshold: "low | medium | high | critical"

outputs:
  - vulnerabilities: array
  - severity_counts: object
  - recommendations: array
  - security_score: number
```

---

### 4.7 🟢 Performance Profiler (NISKI PRIORYTET)

**Funkcjonalność:**
```yaml
skill: performance-profiler
capabilities:
  - Identyfikacja performance bottlenecks
  - Analiza time complexity
  - Memory usage estimation
  - Database query analysis (N+1 detection)
  - Sugerowanie optymalizacji

inputs:
  - file_path: string
  - function_name: string (optional)
  - analyze_queries: boolean

outputs:
  - bottlenecks: array
  - complexity_analysis: object
  - optimization_suggestions: array
```

---

### 4.8 🟢 Migration Assistant (NISKI PRIORYTET)

**Funkcjonalność:**
```yaml
skill: migration-assistant
capabilities:
  - Planowanie migracji (framework, język, wersja)
  - Identyfikacja breaking changes
  - Sugerowanie zmian kodu
  - Generowanie migration checklist
  - Tracking migration progress

inputs:
  - migration_type: "framework | language | version"
  - from: string
  - to: string

outputs:
  - migration_plan: object
  - breaking_changes: array
  - code_changes_needed: array
  - checklist: array
```

---

### 4.9 🟢 Refactoring Planner (NISKI PRIORYTET)

**Funkcjonalność:**
```yaml
skill: refactoring-planner
capabilities:
  - Identyfikacja code smells
  - Sugerowanie refactoring patterns
  - Planowanie refactoringu step-by-step
  - Estymacja ryzyka refactoringu
  - Generowanie refactoring tasks

inputs:
  - file_path: string
  - smell_types: array ["long-method", "god-class", "duplicate-code"]

outputs:
  - code_smells: array
  - refactoring_suggestions: array
  - step_by_step_plan: array
  - risk_assessment: object
```

---

### 4.10 🟢 Database Schema Manager (NISKI PRIORYTET)

**Funkcjonalność:**
```yaml
skill: database-schema-manager
capabilities:
  - Parsowanie schema definitions (SQL, Prisma, TypeORM)
  - Generowanie migrations
  - Wykrywanie schema conflicts
  - Walidacja foreign keys
  - Generowanie ER diagrams

inputs:
  - schema_type: "sql | prisma | typeorm | sequelize"
  - schema_file: string
  - action: "validate | generate_migration | diagram"

outputs:
  - validation_results: object
  - migration_script: string
  - diagram: string (mermaid)
```

---

### 4.11-15: Dodatkowe Skills

11. **Configuration Validator** - walidacja plików konfiguracyjnych
12. **Log Analyzer** - analiza logów aplikacji i wykrywanie wzorców
13. **Code Review Automation** - automatyczne sugestie code review
14. **Changelog Generator** - generowanie changelog z commits
15. **API Contract Validator** - walidacja zgodności z OpenAPI/Swagger

---

## 5. PRIORYTETYZACJA IMPLEMENTACJI

### Faza 1: Foundational Skills (Miesiąc 1-2)
1. **JSON Report Management** - podstawa dla wszystkich agentów
2. **Agent Signature Generator** - traceability
3. **Session Manager** - krytyczne dla orchestracji
4. **Git Operations Manager** - bezpieczeństwo operacji git

### Faza 2: Quality & Validation Skills (Miesiąc 3-4)
5. **Build Validator** - quality gates
6. **File Naming Validator** - spójność
7. **User Parameters Manager** - konfiguracja
8. **Dependency Analyzer** - architektura

### Faza 3: Advanced Skills (Miesiąc 5-6)
9. **Code Metrics Calculator** - jakość kodu
10. **Documentation Generator** - maintainability
11. **Test Coverage Analyzer** - reliability
12. **Security Scanner** - bezpieczeństwo

### Faza 4: Specialized Skills (Miesiąc 7+)
13-15. Skills według potrzeb projektu

---

## 6. SZACUNKOWE KORZYŚCI

### Redukcja duplikacji kodu w agentach:
- JSON Report Management: ~1500 linii
- Agent Signature Generator: ~600 linii
- Session Manager: ~600 linii
- Build Validator: ~1200 linii
- Pozostałe: ~800 linii
**RAZEM: ~4700 linii duplikacji usuniętych**

### Poprawa jakości:
- Spójne raportowanie we wszystkich agentach
- Eliminacja błędów w nazewnictwie sesji
- Automatyczna walidacja buildów
- Lepsza traceability działań

### Łatwiejsze utrzymanie:
- Zmiana w skill = zmiana dla wszystkich agentów
- Łatwiejsze dodawanie nowych agentów
- Centralna walidacja standardów

---

## 7. REKOMENDACJE IMPLEMENTACYJNE

### Architektura Skills:
```
.claude/
├── skills/
│   ├── core/                  # Faza 1
│   │   ├── json-report-manager.md
│   │   ├── agent-signature-generator.md
│   │   ├── session-manager.md
│   │   └── git-ops-manager.md
│   ├── validation/            # Faza 2
│   │   ├── build-validator.md
│   │   ├── file-naming-validator.md
│   │   └── dependency-analyzer.md
│   ├── quality/               # Faza 3
│   │   ├── code-metrics.md
│   │   ├── test-coverage.md
│   │   └── security-scanner.md
│   └── specialized/           # Faza 4
│       ├── migration-assistant.md
│       ├── refactoring-planner.md
│       └── ...
```

### Integracja z agentami:
- Każdy skill powinien mieć jasny interface (inputs/outputs)
- Skills powinny być composable (można ich używać razem)
- Fallback behavior jeśli skill nie jest dostępny
- Versioning skills dla backward compatibility

### Testing:
- Unit testy dla każdego skill
- Integration testy dla interakcji skill-agent
- Regression testy przy zmianach w skills

---

## 8. WNIOSKI

1. **System agentów ma wysoką duplikację** w obszarach:
   - JSON reporting (~30% każdego agenta)
   - Agent signatures (~10% każdego agenta)
   - Build validation (~15% developerów)
   - Session management (~20% orchestratorów)

2. **Skills mogą zredukować tę duplikację o ~35-40%** przy jednoczesnym zwiększeniu spójności

3. **Priorytety:**
   - NAJPIERW: Foundational skills (JSON, signatures, sessions)
   - POTEM: Validation skills (build, naming)
   - NA KONIEC: Advanced/specialized skills

4. **ROI:**
   - Faza 1 skills zwrócą się już po dodaniu 2-3 nowych agentów
   - Każda zmiana standardu będzie wymagała zmiany tylko w skill, nie we wszystkich agentach
   - Nowi agenci będą łatwiejsi do implementacji (mniej boilerplate)

5. **Ryzyko:**
   - Skills mogą wprowadzić single point of failure
   - Wymaga dobrego testowania
   - Może zwiększyć complexity dla nowych developerów
   - Mitigacja: Dobra dokumentacja + fallback behaviors

---

## 9. NASTĘPNE KROKI

1. **Review tej analizy** z zespołem
2. **Prototyp 2-3 skills z Fazy 1** (JSON Report, Signatures, Session Manager)
3. **Pilotaż** - przepisanie 1-2 agentów do używania skills
4. **Ewaluacja** - czy faktycznie przynosi korzyści
5. **Rollout** - stopniowe wprowadzanie kolejnych skills
6. **Iteracja** - dostosowanie na podstawie feedbacku

---

**Podsumowanie:** System ma dużo powtarzalnych wzorców które idealnie nadają się do wyodrębnienia jako skills. Implementacja 10 podstawowych skills może zredukować duplikację o ~4700 linii przy jednoczesnym zwiększeniu spójności i łatwości utrzymania całego systemu.
